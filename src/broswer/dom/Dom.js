
const {
  bindEvent,
  unBindEvent,
  bindOnce,
  transitionEnd
} = require('./BindEvent')

const {
  hasClass,
  addClass,
  removeClass
} = require('./Class')

const {
  createDom
} = require('./CreateDom')

const {
  getStyle,
  setStyle
} = require('./Style')

export default {
  bindEvent,
  unBindEvent,
  bindOnce,
  transitionEnd,
  hasClass,
  addClass,
  removeClass,
  createDom,
  getStyle,
  setStyle
}
