
const {
  bindEvent,
  unBindEvent,
  bindOnce
} = require('./BindEvent')

const {
  hasClass,
  addClass,
  removeClass
} = require('./Class')

const {
  createDom
} = require('./CreateDom')

const {
  getStyle,
  setStyle
} = require('./Style')

export default {
  bindEvent,
  unBindEvent,
  bindOnce,
  hasClass,
  addClass,
  removeClass,
  createDom,
  getStyle,
  setStyle
}
